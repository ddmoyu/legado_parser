[project]
name = "legado"
version = "0.1.0"
description = "阅读 Legado 书源规则的 python 解析库"
readme = "README.md"
authors = [
    { name = "ddmoyu", email = "daydaymoyu@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
